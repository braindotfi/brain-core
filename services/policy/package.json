{
  "name": "@brain/policy",
  "version": "0.0.5",
  "private": true,
  "description": "Rule VM and evaluator. Versioned, signable policies. EIP-712 signing.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "clean": "rm -rf dist *.tsbuildinfo",
    "lint": "eslint src --max-warnings 0",
    "lint:fix": "eslint src --fix",
    "typecheck": "tsc --noEmit -p tsconfig.typecheck.json",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@types/node": "^22.7.0",
    "@types/pg": "^8.11.10",
    "typescript": "^5.6.2",
    "vitest": "^4.1.8",
    "@vitest/coverage-v8": "^4.1.8",
    "eslint": "^9.12.0",
    "fast-check": "^3.23.1"
  },
  "dependencies": {
    "@brain/shared": "workspace:*",
    "fastify": "^5.0.0",
    "pg": "^8.13.0",
    "viem": "^2.21.32",
    "@noble/hashes": "^1.5.0"
  }
}
