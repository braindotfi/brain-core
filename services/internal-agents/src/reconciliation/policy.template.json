{
  "version": 1,
  "rules": [
    {
      "id": "reconciliation_review_auto",
      "applies_to": ["any", "ledger_write"],
      "when": { "agent.role": "reconciliation" },
      "execute": "auto"
    }
  ]
}
