# Risk register

Structured enterprise diligence artifact. For each open risk: the current
mitigation, the owner, the status, the exit criteria, and the evidence
link that lets a diligence reviewer verify the claim themselves.

Updated when a risk's status changes (open / mitigating / closed) or when
exit criteria evolve. Each entry is the answer to "what could go wrong, and
what stops it from going wrong, and how do I know?".

> Audience: enterprise buyers, security reviewers, due-diligence counsel.
> For internal engineering risk tracking, see `docs/audit/index.md` (the
> deeper-detail findings register).

## Open risks

### R-01. External smart-contract audit not yet complete (BrainEscrow)

| Field              | Value                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| ------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Risk               | `BrainEscrow` custodies USDC on Base. Deploying to mainnet before external audit signoff would expose tenant funds to undiscovered contract bugs.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| Current mitigation | (1) Mainnet deploy is **boot-fenced**: `composition/escrow-audit-gate.ts` refuses to start the api when `BRAIN_BASE_CHAIN_ID === 8453 && BRAIN_ESCROW_ADDRESS` is set unless `BRAIN_ESCROW_AUDIT_APPROVED="true"` (operator attestation). (2) CI guard (`check-escrow-audit-marker`) catches the same misconfig at PR-review time before the boot fence has to fire. (3) The contract itself has internal review + Foundry property tests covering 4 invariants. (4) The **deployed on-chain bytecode** is verified at mainnet boot against the audited immutable-masked runtime hash via `eth_getCode` (`assertDeployedEscrowBytecode`); Solidity's immutable byte ranges (the escrow arbiter) are masked before comparison, and `contracts/audit-status.json` carries the masked `runtime_bytecode_sha256` + `immutable_references`. (5) The audit-build evidence (compiler + source-tree + creation/runtime bytecode hashes + `immutable_references` + `approved_chain_ids`) is recomputed from the working tree in CI (`verify-audited-build.mjs`), so a contract or toolchain change after the audited commit fails the build. |
| Owner              | Engineering + external auditor (TBD)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| Status             | **open**. Engagement pending                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| Exit criteria      | (a) external audit completes with no critical or high findings, (b) audited commit SHA + tag pinned in `contracts/AUDIT-SCOPE.md`, (c) deployed mainnet bytecode verified to match audited bytecode (control shipped: the `eth_getCode` boot fence; pending live mainnet confirmation), (d) `BRAIN_ESCROW_AUDIT_APPROVED="true"` enabled in production env                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| Evidence           | `contracts/AUDIT-SCOPE.md`, `services/api/src/composition/escrow-audit-gate.ts`, `scripts/check-escrow-audit-marker.mjs`, `contracts/test/BrainEscrow.t.sol`, `shared/src/bytecode.ts`, `scripts/verify-audited-build.mjs`, `contracts/audit-status.json`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |

### R-02. Tenant blob purge not implemented (GDPR Article 17 gap)

| Field              | Value                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Risk               | `DELETE /v1/tenants/{id}` removes database rows but does not erase the user's raw blob artifacts in Azure Blob Storage (bank statements, invoice PDFs). A user filing an Article 17 demand could receive a "deleted" response while their PII persists.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| Current mitigation | (1) The deletion endpoint surfaces the blob URI list as `blobUrisPendingPurge` on the response + on the `tenant.deleted` audit event, so an operator can run a manual purge pass and the act of "not yet erased" is on-chain. (2) RFC 0003 phase B shipped: tenant deletion enqueues a durable `tenant_blob_purge_jobs` row in-transaction; a privileged worker drains it via `BlobAdapter.purgeTenant` (version-aware permanent erasure: S3 all-versions + delete markers, Azure versions + snapshots) with backoff + dead-letter + legal-hold surfacing + lifecycle audit events, restricted to the worker by the `check-blob-purge-callsite` CI guard. (3) 2026-06-07 review hardening shipped: crash-safe lease recovery (stale `purging` jobs reclaimed past a lease timeout; every status write fenced on a unique lock token), structured failure classification (transient / authorization / unknown errors are retried; only a confirmed WORM / object-lock is the terminal `blocked_legal_hold`, so a 503 no longer masquerades as a legal hold), and a transactional audit outbox (each transition writes its audit intent in the same transaction and a publisher delivers it idempotently, removing the emit-before-write orphan and the `audit-emit-failed` sentinel). |
| Owner              | Engineering                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| Status             | **mitigating**. Phase B + 2026-06-07 hardening shipped (durable queue + privileged worker + version-aware S3/Azure erasure + lease recovery + failure classification + transactional audit outbox + call-site guard). An end-to-end erasure test now drives `runBlobPurgeCycle` against a real `MemoryBlobAdapter`, proving the deleted tenant's bytes are erased and a co-resident tenant's survive (not a mocked `purgeTenant`); a live-cloud (localstack/S3) permanent-erasure integration test on the deployed adapters remains                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| Exit criteria      | Phase B ships per RFC 0003 §5: (a) `tenant_blob_purge_jobs` migration, (b) `BlobAdapter.purge()` on memory/s3/azure, (c) `tenant-blob-purge-worker.ts`, (d) `check-blob-purge-callsites.mjs` lint guard, (e) integration test proving end-to-end erasure                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| Evidence           | `docs/rfcs/0003-blob-purge-article-17.md`, `services/api/src/tenant-deletion/blob-purge-worker.ts`, `services/api/src/tenant-deletion/blob-purge-repo.ts`, `services/api/src/tenant-deletion/blob-purge-audit-outbox.ts`, `shared/src/blob/purge-classify.ts`, `services/api/src/tenant-deletion/service.test.ts`, `services/api/src/tenant-deletion/blob-purge-worker.test.ts`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |

### R-03. Staging VM deploy chain not yet exercised

| Field              | Value                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| ------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Risk               | The codebase ships GitHub Actions workflows for build, staging deploy, and production promote, but the staging VM, staging secrets, host env files, and GitHub production environment gate still need operator provisioning. Production-readiness claims are theoretical until this chain runs against live staging.                                                                                                                                                   |
| Current mitigation | Disclosed explicitly in `architecture/enterprise-readiness.md` under "Blockers for unrestricted mainnet production." All other safety controls (boot fences, gate, audit, RLS) are runtime-independent and would survive any deploy substrate. `docs/r03-staging-deploy-runbook.md` gives the ordered VM path: build one image, deploy staging on green main, smoke the staging commit, then require GitHub production environment approval before production promote. |
| Owner              | Operations (TBD)                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| Status             | **open**. Operational milestone                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| Exit criteria      | (a) staging VM secrets configured (`VM_HOST_STAGING`, `VM_SSH_KEY_STAGING`), (b) build → deploy → staging-validation chain runs cleanly end-to-end, (c) production deploy logs show all 5 boot fences armed via `brain.runtime.capabilities` line, (d) rollback procedure exercised against staging                                                                                                                                                                    |
| Evidence           | `.github/workflows/main.yml`, `architecture/enterprise-readiness.md`, `docs/rollback.md`, `docs/r03-staging-deploy-runbook.md`                                                                                                                                                                                                                                                                                                                                         |

### R-04. Full money-movement E2E not yet enforced in CI

| Field              | Value                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| ------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Risk               | `tests/e2e/signed-agent-gated-payment.e2e.test.ts` asserts §6 gate dormancy when staging history exists but skips when fixtures are missing. The full chain (propose → gate → audit-before → outbox → rail → receipt → audit-after → proof verify) is not enforced as a per-PR signal.                                                                                                                                                                                                         |
| Current mitigation | (1) `scripts/demo/golden-path.sh` runs the full chain end-to-end with `BRAIN_DEMO_STRICT_PROOF=true` blocking until proof verifies. (2) §6 gate has property + unit + integration tests under `shared/src/gate/`. (3) Per-check metrics surface dormancy via `brain.gate.outcome.count{outcome=not_applicable}`.                                                                                                                                                                               |
| Owner              | Engineering + infra                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| Status             | **mitigating**. A per-PR `money_movement_e2e` job enforces the full chain + §6 gate-check coverage (8/9.5/11.5 `not_applicable` fails the build) + 3 negatives (duplicate, policy-reject, missing-approval); audit-before → after linkage is now asserted in strict mode (proof must contain `execute.before` preceding `execute.after`, the after chained via `prev_event_hash`). Remaining: localstack-in-CI for the blob path + strict on-chain anchor mode (needs a CI-available Base RPC) |
| Exit criteria      | (a) deterministic local-stack-in-CI (pg + redis + localstack + boot binary in GH Actions), (b) the golden-path demo runs in strict mode on every PR, (c) any check 8 / 9.5 / 11.5 going `not_applicable` fails the build, (d) audit-before → audit-after linkage asserted                                                                                                                                                                                                                      |
| Evidence           | `tests/e2e/signed-agent-gated-payment.e2e.test.ts`, `scripts/demo/golden-path.sh`, `.github/workflows/pr.yml`, `infra/grafana/gate.json`                                                                                                                                                                                                                                                                                                                                                       |

_R-06 and R-07 were closed in batch 8 P2; see the "Recently closed" section below._

### R-08. Money-touching rail-adapter test coverage gap

| Field              | Value                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| ------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Risk               | `onchainExecutor` (viem), `plaidClient` (Plaid SDK), `x402Client`, and `PostgresSourceRepository` were flagged as thinly exercised by Opus 4.8. The §6 gate ahead of them is well-tested; the adapters themselves needed parity.                                                                                                                                                                                                                                                                                                                                           |
| Current mitigation | **Batch 8 P5**: error-path tests added across three rail adapters. `onchainExecutor` covers viem read/write error propagation + receipt-hash sourcing (9 tests, was 4). `plaidClient` covers Plaid API errors verbatim + env mapping (6 tests, was 2). `x402Client` has a new test file covering the full 4-step lifecycle: decimals read, transfer revert, receipt timeout, facilitator 5xx + network error (best-effort), idempotency-key forwarding (8 tests, new). `PostgresSourceRepository` was already at 16-test coverage. 23 adapter unit tests now pass (was 6). |
| Owner              | Engineering                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| Status             | **mitigating** (P2). Per-file >=80% line/function/statement coverage gates are now wired into `services/api/vitest.config.ts` for each rail adapter (onchainExecutor, plaidClient, x402Client), so a single adapter regressing below 80% fails CI even while the aggregate stays high (verified by forcing a gate above the real coverage). First exit criterion met; the testnet integration for the onchain executor now has gated scaffolding (see R-14), runnable once testnet fixtures are provisioned                                                                |
| Exit criteria      | Coverage parity with the §6 gate measured at >=80% line coverage on the adapter files (DONE: per-file gates wired into `test:coverage`); integration tests on testnet for at least the onchain executor end-to-end (scaffolding shipped in R-14; runs once testnet fixtures/secrets are provisioned)                                                                                                                                                                                                                                                                       |
| Evidence           | `services/api/vitest.config.ts`, `services/api/src/rails/onchainExecutor.test.ts`, `services/api/src/rails/plaidClient.test.ts`, `services/api/src/rails/x402Client.test.ts`, `services/raw/src/sources/PostgresSourceRepository.test.ts`                                                                                                                                                                                                                                                                                                                                  |

### R-14. On-chain executor had only unit/mocked coverage (no testnet E2E)

| Field         | Value                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| ------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Risk          | The on-chain executor (`buildOnchainExecutor` + `OnchainBaseRail`) was only unit/mocked, so "the on-chain rail works against the deployed contract" was asserted, not proven (the deferred half of R-08's exit criteria).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| Resolution    | A testnet-gated E2E (`tests/e2e/onchain-executor.testnet.e2e.test.ts`) drives the **real** `OnchainBaseRail` against a deployed `BrainSmartAccount` on Base Sepolia: read path (nonce), revert path (a real on-chain revert surfaces as `execution_rail_declined`, gas only), and a double-gated success case (granted-key fixture) that executes once then asserts the on-chain replay guard directly (a re-send at the **consumed** nonce reverts with `BadNonce`; the prior assertion that re-called `rail.dispatch` was corrected per Review-053af35 P1, since it read a fresh nonce and could not prove exactly-once; outbox idempotency is proved separately in the execution outbox suite). Opt-in CI job `testnet_onchain_executor_e2e` (gated on `vars.TESTNET_ONCHAIN_E2E_ENABLED`); the test self-skips and the job is var-gated, so a green check means a real run. `production-readiness` reports an "On-chain executor testnet E2E" row (yellow until wired). **Mitigating, not closed**: scaffolding is shipped + ready; actual execution needs provisioned testnet fixtures (overlaps the deferred R-03 substrate). |
| Status        | **mitigating** (P2). Scaffolding shipped; full execution pending provisioned testnet fixtures.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| Exit criteria | The gated job runs green against a provisioned Base Sepolia `BrainSmartAccount` (read + revert cases at minimum; success case with a granted-key fixture), and `production-readiness` reports the row green.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| Evidence      | `tests/e2e/onchain-executor.testnet.e2e.test.ts`, `.github/workflows/main.yml` (`testnet_onchain_executor_e2e`), `scripts/production-readiness.mjs`, `tests/e2e/README.md`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |

## Recently closed

### R-13. All background workers run inside the public API process (closed 2026-06-19)

| Field      | Value                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| ---------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Risk       | The monolithic `main()` ran ten background workers (ingestion, canonical + ledger projection, execution-outbox drain, webhook dispatch, blob purge, audit verifier + anchor scheduler) inside the same process that serves the public HTTP API: an api deploy/restart bounced ingestion + execution, horizontal api scaling multiplied workers, and the public api runtime held every worker's privileged DB credential.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| Resolution | One-image, role-via-env separation: `BRAIN_HTTP_ENABLED` gates the `/v1` surface (health always on); `BRAIN_WORKERS` (`all`/`none`/CSV of nine groups) selects which workers a process runs. `composition/process-roles.ts` maps env -> {`/v1`, workers, least-privilege role pools}; the db-isolation fence + boot role-assertion only require/check the pools a process uses, and an unset role URL aliases `brain_app`, so an api process holds no worker DB credentials. Defaults reproduce the all-in-one process; `docker-compose.prod.yml` runs an `api` (workers=none) + a `worker` (http off, workers=all) off one image. Per-worker advisory leases (`leasedCycle`, a per-worker `pg_try_advisory_lock`) now make the canonical/ledger/normalize/webhook workers multi-replica safe (one active at a time, crash failover within one interval). Deferred to R-03: per-group autoscaling with per-item parallelism. |
| Status     | **closed** 2026-06-19                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| Evidence   | `services/api/src/composition/process-roles.ts` (+ `.test.ts`), `services/api/src/main.ts`, `services/api/src/composition/db-isolation.ts`, `services/api/src/db-isolation-fences.test.ts`, `docker-compose.prod.yml`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |

### R-12. Single broad `brain_privileged` BYPASSRLS role for all cross-tenant API work (closed 2026-06-19)

| Field      | Value                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| ---------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Risk       | The API runtime used ONE broad `brain_privileged` BYPASSRLS role for every cross-tenant background job and resolver, so a confused-deputy bug or compromise in any one privileged path had the cross-tenant power of all of them (raw, canonical, ledger, audit, execution, tenant-deletion).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| Resolution | Replaced with eight least-privilege BYPASSRLS roles (`infra/db-roles.sql` §4), each granted only its layer's tables: `brain_raw_worker`, `brain_canonical_projector`, `brain_ledger_projector` (writes only the three projection targets, not money-path `ledger_*`), `brain_execution_worker` (outbox claim/mark only), `brain_audit_verifier`, `brain_audit_publisher` (read only), `brain_resolver` (read only), `brain_tenant_deletion` (GDPR erasure, route-gated; audit preserved). Each has its own `BRAIN_*_DB_URL` (all eight required in production, fenced by `db-isolation.ts`); the boot-time `assertDbRoles` proves each pool connects as its role with a forbidden-privilege list. `brain_privileged` no longer runs in the API runtime (seed one-shot only). A runtime-as-role integration test (`db-role-grants.integration.test.ts`) applies the real db-roles.sql and uses SET ROLE to prove each role both holds its grants and lacks the forbidden ones (CI, superuser DATABASE_URL). |
| Status     | **closed** 2026-06-19                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| Evidence   | `infra/db-roles.sql`, `services/api/src/composition/db-isolation.ts`, `services/api/src/main.ts`, `services/api/src/composition/db-roles-sql.test.ts`, `services/api/src/db-isolation-fences.test.ts`, `tests/invariants/integration/db-role-grants.integration.test.ts`, `docker-compose.prod.yml`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |

### R-09. On-chain tumbling vs off-chain rolling spend windows can diverge (closed 2026-06-19)

| Field      | Value                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Risk       | `BrainSmartAccount` keys its session-key budget on a tumbling window (`(ts/period)*period`); the off-chain agent-window-spend loader previously used a trailing rolling window, which could diverge at boundaries (an off-chain budget check permitting a spend the on-chain cap rejects, or vice versa).                                                                                                                                                                                                                                                                                                                                                                            |
| Resolution | Batch 8 P3 switched the off-chain loader to the same tumbling formula. This close adds the proof: `tumblingWindowStartSeconds` is the single canonical formula both enforcers mirror, and fast-check property tests (`agent-window-spend.windows.test.ts`) prove it equals an INDEPENDENT derivation of the on-chain Solidity integer-division window (`ts - ts%p`) at T-1/T/T+1, that the off-chain double-precision epoch intermediate does not diverge across the realistic epoch range, and the containment/monotonicity invariants. Approve-as-spend is NA off-chain (only `executed` intents are summed); the optional on-chain rationalization stays deferred (conservative). |
| Status     | **closed** 2026-06-19                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| Evidence   | `services/policy/src/agent-window-spend.ts`, `services/policy/src/agent-window-spend.windows.test.ts`, `contracts/src/BrainSmartAccount.sol:335-380`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |

### R-11. Canonical cutover moved the obligation money-path from synchronous to two async workers (closed 2026-06-19)

| Field      | Value                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| ---------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Risk       | Phase 5 (RFC 0005) made `ledger_obligations`/`counterparties` a projection of canonical; an obligation reaches the Ledger via two scheduled workers (`raw_parsed`->canonical->Ledger) rather than synchronously. A stalled worker would silently keep obligations out of the Ledger, leaving the §6 gate nothing to gate.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| Resolution | (i) e2e integration test drives BOTH worker cycles to the Ledger; (ii) each worker emits a records-projected counter AND a lag gauge (`brain.canonical.projector.lag_seconds`, `brain.ledger.apar_projection.lag_seconds`); (iii) `infra/grafana/projection.json` defines ProjectionPipelineStalled (lag>600s + zero throughput) and ProjectionLagHigh (lag>300s) alerts; (iv) projections are idempotent + replayable; (v) poison-message handling (`canonical/0003`): a single malformed record is retried up to `maxAttempts` (default 5) then quarantined so it cannot wedge or silently drop a lane, surfaced via `brain.canonical.projector.quarantine.count`/`.quarantine.depth`, with `replayQuarantined()` to drain after a fix. The optional in-process liveness probe is unnecessary given the stall alert. |
| Status     | **closed** 2026-06-19                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| Evidence   | `tests/invariants/integration/canonical-pipeline.integration.test.ts`, `services/canonical/src/projectors/worker.ts`, `services/canonical/migrations/0003_canonical_projection_quarantine.sql`, `services/canonical/src/__integration__/projector.integration.test.ts`, `services/ledger/src/projection/obligations.ts`, `infra/grafana/projection.json`                                                                                                                                                                                                                                                                                                                                                                                                                                                               |

### R-06. BrainSmartAccount session-key caps unit-blind to ERC20 decimals (closed batch 8 P2)

| Field              | Value                                                                                                                                                                                                                                                                                                                                            |
| ------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Risk               | `SessionKey.maxPerTx` / `maxPerPeriod` were bare `uint256` with no token-unit context; the executor decoded raw ERC20 amounts (USDC=6dp, DAI=18dp) and compared them against a cap intended in wei. Unit-blind.                                                                                                                                  |
| Current mitigation | Closed in code (commit `6190ffe`): `SessionKey` now carries `capToken`. In ERC20 mode (`capToken != 0`) caps are in capToken's raw units; `grantSessionKey` enforces `allowedTargets == [capToken]` and selector subset; `executeViaSessionKey` verifies target match and `value == 0`. Foundry tests for 6dp USDC + 18dp DAI prove enforcement. |
| Status             | **closed** in batch 8 P2                                                                                                                                                                                                                                                                                                                         |
| Evidence           | `contracts/src/BrainSmartAccount.sol:13-50` (struct + selector constants), `:140-179` (ERC20-mode grant validation), `:266-302` (executor dual mode); `contracts/test/BrainSmartAccount.t.sol` (R-06 tests); `contracts/AUDIT-SCOPE.md:131-145`                                                                                                  |
| Notes              | Mainnet enablement of escrow / on-chain rails still gated on R-01 (external audit). R-06 closure is at the code level; the audit is the separate trust gate.                                                                                                                                                                                     |

### R-07. BrainSmartAccount non-decodable selectors bypass caps (closed batch 8 P2)

| Field              | Value                                                                                                                                                                                                                                                        |
| ------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Risk               | Selector decode covered only `{transfer, approve, transferFrom}`. Any other allowlisted selector left `capAmount=0` and passed every cap unmetered.                                                                                                          |
| Current mitigation | Closed in code (commit `6190ffe`): `grantSessionKey` rejects any selector outside the decodable set when `capToken` is set (new error `NonDecodableSelectorInErc20Mode`). The contract refuses to grant a session key whose caps would silently be bypassed. |
| Status             | **closed** in batch 8 P2                                                                                                                                                                                                                                     |
| Evidence           | `contracts/src/BrainSmartAccount.sol:148-179`; `contracts/test/BrainSmartAccount.t.sol` (`test_R07_grant_rejectsNonDecodableSelectorInErc20Mode`, `test_R07_grant_rejectsTargetMismatchInErc20Mode`); `contracts/AUDIT-SCOPE.md:131-145`                     |
| Notes              | As with R-06, mainnet enablement still gated on R-01.                                                                                                                                                                                                        |

### R-10. BrainSmartAccount NatSpec framing (closed batch 8 P2)

| Field              | Value                                                                                                                                                                                                                                           |
| ------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Risk               | NatSpec framing as "ERC-4337-style" misleads reviewers and integrators. The contract is a directly-called session-key executor with no EntryPoint, UserOperation, or paymaster.                                                                 |
| Current mitigation | `BrainSmartAccount.sol` contract NatSpec rewritten: now describes the actual design ("Smart account with directly-called session keys. Not ERC-4337: there is no EntryPoint, no UserOperation, no paymaster"). `AUDIT-SCOPE.md` mirror updated. |
| Status             | **closed** in batch 8 P2                                                                                                                                                                                                                        |
| Evidence           | `contracts/src/BrainSmartAccount.sol:4-30`, `contracts/AUDIT-SCOPE.md:131-145`                                                                                                                                                                  |

### R-05. Rail catalog vs docs drift (closed batch 6 P1)

| Field              | Value                                                                                                                                                                                                                                                 |
| ------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Risk               | The rail catalog (`services/api/src/composition/rail-catalog.ts`) is the runtime source of truth; the rails matrix doc (`docs/rails-matrix.md`) is the buyer-facing reference. Drift between them would damage diligence trust.                       |
| Current mitigation | `check-rails-catalog-drift` CI guard (wired into `pnpm run lint`) compares both files and fails on any divergence in name, `productionAllowed`, `auditRequired`, or `requiredEnv` set. Two parsers share a contract so format changes break together. |
| Status             | **closed**. Drift is now structurally impossible at PR merge time                                                                                                                                                                                     |
| Evidence           | `scripts/check-rails-catalog-drift.mjs`, the script's unit-test fixtures simulate every drift class                                                                                                                                                   |

## Risks NOT in this register

This register tracks **enterprise-diligence-facing** risks. Internal
engineering items (refactor backlog, technical debt, deprecation
schedules) live in `docs/audit/index.md` and the project task tracker.
The two registers don't compete; they answer different questions:

| Register                | Question it answers                                                  |
| ----------------------- | -------------------------------------------------------------------- |
| `docs/risk-register.md` | What could a buyer or auditor be concerned about, and what stops it? |
| `docs/audit/index.md`   | What does the engineering team need to fix or refactor before v0.4?  |

A risk graduates from internal to enterprise-facing when it has
diligence-blocking potential (custody safety, GDPR compliance, deploy
provability, contract security).
