{
  "name": "@brain/plaid-sandbox",
  "version": "0.1.0",
  "private": true,
  "description": "Pulls transactions from Plaid Sandbox and ingests them into Brain's raw layer.",
  "type": "module",
  "scripts": {
    "build": "tsc -b",
    "clean": "rm -rf dist *.tsbuildinfo",
    "typecheck": "tsc -b --noEmit",
    "start": "tsx src/index.ts"
  },
  "dependencies": {
    "plaid": "^18.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.7.0",
    "typescript": "^5.6.2"
  }
}
