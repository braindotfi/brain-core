{
  "name": "@brain/dev-token",
  "version": "0.1.0",
  "private": true,
  "description": "CLI to mint short-lived dev JWTs for local Brain API testing.",
  "type": "module",
  "scripts": {
    "build": "tsc -b",
    "clean": "rm -rf dist *.tsbuildinfo",
    "typecheck": "tsc -b --noEmit",
    "start": "tsx src/index.ts"
  },
  "dependencies": {
    "jose": "^5.9.3"
  },
  "devDependencies": {
    "@types/node": "^22.7.0",
    "typescript": "^5.6.2"
  }
}
